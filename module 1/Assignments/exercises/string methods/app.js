function capLower(str1) {
    console.log(str1 + hello);

}

var hello = "hello";
var world = "hello world";
var upper = hello.toUpperCase();
var upper1 = world.toUpperCase();
capLower(upper);

function strLength(str2) {
    console.log(str2);
}

var length = hello.length;
var math = Math.floor(length / 2);
var slice = hello.slice(0, 2);
strLength(math);

function str1Length(str3) {
    console.log(str3);
}

var length1 = world.length;
var math1 = Math.floor(length1 / 2)
var slice1 = world.slice(0, 5);
str1Length(math1);

function strSlice(str4) {
    console.log(str4);
}
strSlice(slice);

function str1Slice(str5) {
    console.log(str5);
}
str1Slice(slice1);

function strJoin(str6) {
    console.log(str6);
}

var slice2 = upper.slice(0, 2);
var cut = hello.substring(2);
var join = slice2 + cut;
strJoin(join);

function strJoin1(str7) {
    console.log(str7);
}

var slice3 = upper1.slice(0, 5);
var cut1 = world.substring(5);
var join1 = slice3 + cut1;
strJoin1(join1);